---
interface Props {
  text: string;
  class?: string;
}
const { text, class: className = '' } = Astro.props;
---

<div class:list={['label', className]}>{text}</div>

<style>
  /* .label стили — глобальные в global.css */
</style>
